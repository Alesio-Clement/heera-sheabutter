RewriteEngine On
# Remove .php extension from URL
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^(.*)$ $1.php [L]

# Redirect .php to clean URL (optional, but recommended for SEO)
RewriteCond %{THE_REQUEST} \s([^.]+\.php)[\s?] [NC]
RewriteRule ^ %1 [R=301,L]